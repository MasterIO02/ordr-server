/*
 * This extension is called when a render errored (has an error code), the server doesn't have anything more to do with it.
 */

module.exports = async render => {
    // render is a mongoose document, that you can modify and save easily by directly changing the object data and calling .save() on it.
}
